# What's new in V5?

- This v5 is entirely geared towards Poetry that combines virtual environments & dependencies management into a unique package.

So we could have the same workflow than v4 with Poetry for linting, formatting and testing but we don't do it.

Just to show a workflow with an augmented tool like Poetry or uv which look like more to modern workflows even if it's usefull to know how pip and virtual environment package managers work.